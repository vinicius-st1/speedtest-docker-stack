# Copie para inventory.private.yml e ajuste conteúdos sensíveis.
# Este arquivo NÃO deve ser versionado com dados reais.
# IMPORTANTE: cada instância do inventory.yml deve ter seu bloco ookla.properties_raw aqui.
instances:
  - name: "speedtest01"
    ookla:
      properties_raw: |
        # Exemplo de bloco completo fornecido pelo Ookla
        # key=value

  - name: "speedtest02"
    ookla:
      properties_raw: |
        # Exemplo de bloco completo fornecido pelo Ookla
        # key=value

  - name: "speedtest03"
    ookla:
      properties_raw: |
        # Exemplo de bloco completo fornecido pelo Ookla
        # key=value

  - name: "speedtest04"
    ookla:
      properties_raw: |
        # Exemplo de bloco completo fornecido pelo Ookla
        # key=value
